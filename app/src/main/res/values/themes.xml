<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Book_Store_MobileApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Book_Store_MobileApp" parent="Base.Theme.Book_Store_MobileApp" />

    <!-- ✅ Thêm theme splash riêng cho SplashActivity -->
    <style name="Theme.Book_Store_MobileApp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_splash_logo_book</item>
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="postSplashScreenTheme">@style/Theme.Book_Store_MobileApp</item>
    </style>
</resources>
